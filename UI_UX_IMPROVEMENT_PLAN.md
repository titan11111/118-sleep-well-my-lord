# 🎮 殿、ご安眠を。 — UI/UX改善計画（30項目）

## 目標
**プレイ画面を300%改善** — 特にプレイ画面（Canvas）の表示サイズが小さい問題を解決

---

## 動かない理由と修正内容（2025対応）

### 動かなかった主な原因
1. **タイムライン初期化の不整合**  
   `game.js` の `initializeTimeline()` が `stage.waves` を参照していましたが、`config.js` のステージデータは **`stage.enemies`**（波ごとの配列の配列）のみ定義されています。  
   → `stage.waves` が `undefined` のためタイムラインが計算されず、シミュレーションや敵予測が動いていませんでした。

2. **シナリオ生成時のエラー**  
   `generateTrapScenario()` 内で **`Math.seedrandom`** を使用していましたが、このライブラリは読み込まれていません。  
   → ステージ開始時に `updateScenarioPanel()` → `generateTrapScenario()` が呼ばれ、ここで例外が発生してゲームが止まっていました。

### 実施した修正
- **game.js**: `initializeTimeline()` で `stage.enemies` を参照し、タイムライン用に `{ enemies: waveArray }` 形式に変換して渡すように変更。
- **game.js**: `generateTrapScenario()` を、`Math.seedrandom` に依存しない簡易シード付き乱数で置き換え。
- **game-timeline.js**: 敵種別の弱点・推奨罠判定を、日本語名（忍び・大男など）ではなく config のキー（`ninja`, `brute` など）で行うように修正。

---

## 【優先度 A】 ゲーム画面の拡大（基本改善）

### 1. Canvas サイズを動的に拡大
- 現在: 固定 600×400px
- 改善: 画面の80%まで動的拡大（最大1200×800px）
- **影響度: ★★★★★ （最重要）**

### 2. レスポンシブ Grid レイアウト導入
- 現在: Flexbox で横並び
- 改善: 画面幅に応じて Canvas を上部に、UI を下部に配置
- **影響度: ★★★★☆**

### 3. UIレイアウトの再構成
- 現在: Top UI, Bottom UI が重なっている
- 改善: Side panel + Bottom panel に分離
- **影響度: ★★★★☆**

### 4. Game Container の高さ最適化
- 現在: max-height: 95vh のみ
- 改善: Aspect ratio を保ちながら全体をスケール
- **影響度: ★★★☆☆**

---

## 【優先度 B】 UI要素の見やすさ向上

### 5. ステータス表示をより大きく
- 現在: font-size 1.1em
- 改善: Stage/Money/Timer を 1.3em → 1.5em に拡大
- **影響度: ★★★★☆**

### 6. スリープゲージを拡大・強調
- 現在: 200×25px
- 改善: 動的に幅 60%まで拡大、高さ 40px に
- **影響度: ★★★★☆**

### 7. トラップボタンのサイズ向上
- 現在: min-width 70px
- 改善: min-width 100px 以上に、大きなアイコン（3em）
- **影響度: ★★★★☆**

### 8. メッセージ表示エリアの拡張
- 現在: min-height 30px
- 改善: 複数行対応、背景パネル追加
- **影響度: ★★★☆☆**

### 9. ボタンの一貫性向上
- 現在: btn, trap-btn で異なるスタイル
- 改善: 統一デザイン + ホバー効果強化
- **影響度: ★★★☆☆**

---

## 【優先度 C】 機能的な UI 改善

### 10. トラップパレットの折り返し対応
- 現在: flex-wrap で複数行になると見づらい
- 改善: スクロール可能な横スクロール トラップパレット
- **影響度: ★★★☆☆**

### 11. 左近の独白/殿の寝言を改善
- 現在: min-height 1.3em/1.4em で圧縮
- 改善: 背景パネル + より大きなテキスト
- **影響度: ★★☆☆☆**

### 12. スカウト情報（scout-info）の改善
- 現在: max-height 80px で切れる
- 改善: 展開可能なパネルに
- **影響度: ★★☆☆☆**

### 13. ステージ選択画面のグリッド改善
- 現在: auto-fit minmax(200px)
- 改善: 最小 250px + より大きなカード
- **影響度: ★★★☆☆**

### 14. ステージカードのハイライト
- 現在: scale(1.02) ホバー
- 改善: shadow + border glow + animation
- **影響度: ★★☆☆☆**

### 15. タイトル画面のストーリーテキスト
- 現在: max-height 300px、スクロール
- 改善: より読みやすいフォント + 行間
- **影響度: ★★☆☆☆**

---

## 【優先度 D】 ビジュアル改善

### 16. 全体的なフォント サイズベース を上げる
- 現在: body は 1.1em
- 改善: 1.2em を default に
- **影響度: ★★★☆☆**

### 17. ボタン padding の統一
- 現在: btn は 12px 25px
- 改善: 16px 30px に拡大
- **影響度: ★★☆☆☆**

### 18. Shadow 効果の強化
- 現在: subtle な shadow
- 改善: より深い shadow で立体感
- **影響度: ★★☆☆☆**

### 19. Glow/Highlight 効果
- 現在: rgba color が薄い
- 改善: より明るいハイライト効果
- **影響度: ★★☆☆☆**

### 20. Color Contrast の改善
- 現在: 一部テキストが濃度が低い
- 改善: より高いコントラスト比
- **影響度: ★★★☆☆**

---

## 【優先度 E】 インタラクション改善

### 21. ツールチップ表示
- 現在: ない
- 改善: トラップボタン hover で cost と説明を表示
- **影響度: ★★☆☆☆**

### 22. キーボード ショートカット
- 現在: ない
- 改善: 数字キー（1-9）でトラップ選択
- **影響度: ★☆☆☆☆**

### 23. ホットキーのビジュアル表示
- 現在: ない
- 改善: ボタンに [1][2][3] キー表示
- **影響度: ★☆☆☆☆**

### 24. マウス ホバーの座標表示
- 現在: 座標が見えない
- 改善: Canvas 上にグリッド座標を表示
- **影響度: ★★☆☆☆**

---

## 【優先度 F】 モバイル 対応改善

### 25. Mobile での Canvas 拡大
- 現在: tablet サイズでもやや小さい
- 改善: 100% width (max 100vh にclip)
- **影響度: ★★★☆☆**

### 26. Mobile ボタンのタッチサイズ
- 現在: 70px では小さい
- 改善: min-width 90px, padding 12px 20px
- **影響度: ★★☆☆☆**

### 27. Mobile UI の縦配置
- 現在: 横並び flex で圧縮される
- 改善: モバイル時は max-width 100% で全幅使用
- **影響度: ★★☆☆☆**

### 28. Mobile での field name 短縮
- 現在: "ステージ:", "💰 G" など
- 改善: Mobile 時は省略表記に
- **影響度: ★☆☆☆☆**

---

## 【優先度 G】 その他の改善

### 29. Loading 画面 / Transition
- 現在: 瞬間的に画面切り替わる
- 改善: Fade animation で transitions 追加
- **影響度: ★☆☆☆☆**

### 30. アクセシビリティ向上
- 現在: aria-* がない
- 改善: button に aria-label, role 追加
- **影響度: ★☆☆☆☆**

---

## 実装スケジュール

| 優先度 | 項目数 | 予想所要時間 |
|--------|--------|------------|
| A | 4 | 60分（基本構造） |
| B | 6 | 45分 |
| C | 5 | 40分 |
| D | 5 | 35分 |
| E | 4 | 25分 |
| F | 4 | 30分 |
| G | 2 | 15分 |
| **合計** | **30** | **250分（≒4時間）** |

---

## 実装方針

1. **優先度 A を先に完了** → プレイ画面の300%拡大を実現
2. **B → C を並行** → 一気に見栄えを改善
3. **D以降は調整** → 細部のフォーマット

**各改善後は、実機確認 + スクリーンショット撮影して検証します**

